version: 2
task_description: Write Actian 4GL OpenROAD If-Then-Else Statement
created_by: willsjames
seed_examples:
  - question: How to use a basic if-then-endif statement in Actian 4GL OpenROAD If Then
      Else Statement?
    answer: "Here's a basic if-then-endif statement:\n```\nif empnum = 0 then\n    employee_number\
      \ = 'None';\nendif;\n```\nThis code sets employee_number to 'None' if empnum is\
      \ 0."
  - question: Write code to prompt for user input using Actian 4GL OpenROAD If Then
      Else Statement.
    answer: "Here's code to prompt for user input:\n```\nif empnum = 0 then\n    enum\
      \ = prompt 'Please enter employee number';\nendif;\n```\nThis code prompts the\
      \ user to enter an employee number if empnum is 0."
  - question: How to implement an if-then-elseif-then-else-endif structure in Actian
      4GL OpenROAD If Then Else Statement?
    answer: "Here's an if-then-elseif-then-else-endif structure:\n```\nif status = 'n'\
      \ then\n    callframe new;\nelseif status = 'c' then\n    callframe completed;\n\
      else\n    callframe inprogress;\nendif;\n```\nThis code calls different frames\
      \ based on the value of status."
  - question: Write code to nest multiple if statements in Actian 4GL OpenROAD If Then
      Else Statement.
    answer: "Here's an example of nested if statements:\n```\nif status = 'n' then\n\
      \    if empnum = 0 then\n        employee_number = 'None';\n    else\n       \
      \ callframe NewEmp;\n    endif;\nendif;\n```\nThis code nests an if statement\
      \ inside another, checking both status and empnum."
  - question: How to use the if-then-else-endif variant in Actian 4GL OpenROAD If Then
      Else Statement?
    answer: "Here's an example of the if-then-else-endif variant:\n```\nif age >= 18\
      \ then\n    status = 'Adult';\nelse\n    status = 'Minor';\nendif;\n```\nThis\
      \ code sets the status based on the age, using both 'then' and 'else' clauses."
  - question: Write code to check multiple conditions in an Actian 4GL OpenROAD If Then
      Else Statement.
    answer: "Here's code checking multiple conditions:\n```\nif (age >= 18 and status\
      \ = 'active') or (age >= 65 and status = 'retired') then\n    eligible = TRUE;\n\
      else\n    eligible = FALSE;\nendif;\n```\nThis code sets 'eligible' based on multiple\
      \ conditions involving age and status."
  - question: How to use the if statement for basic error handling in Actian 4GL OpenROAD
      If Then Else Statement?
    answer: "Here's an example of using if for basic error handling:\n```\nif db_status\
      \ != 0 then\n    message 'Database error occurred';\n    rollback;\nelse\n   \
      \ commit;\n    message 'Transaction successful';\nendif;\n```\nThis code checks\
      \ db_status and performs actions based on whether an error occurred."
  - question: Write code to validate user input using Actian 4GL OpenROAD If Then Else
      Statement.
    answer: "Here's code to validate user input:\n```\ninput_value = prompt 'Enter a\
      \ number between 1 and 10';\nif input_value >= 1 and input_value <= 10 then\n\
      \    message 'Valid input';\nelse\n    message 'Invalid input. Please try again.';\n\
      endif;\n```\nThis code checks if the input is within the specified range."
  - question: How to use multiple elseif conditions in Actian 4GL OpenROAD If Then Else
      Statement?
    answer: "Here's an example with multiple elseif conditions:\n```\nif score >= 90\
      \ then\n    grade = 'A';\nelseif score >= 80 then\n    grade = 'B';\nelseif score\
      \ >= 70 then\n    grade = 'C';\nelse\n    grade = 'F';\nendif;\n```\nThis code\
      \ assigns a grade based on the score using multiple elseif conditions."
  - question: Write code to implement a simple menu system using Actian 4GL OpenROAD
      If Then Else Statement.
    answer: "Here's a simple menu system implementation:\n```\nchoice = prompt 'Enter\
      \ your choice (1-3)';\nif choice = '1' then\n    callframe CustomerInfo;\nelseif\
      \ choice = '2' then\n    callframe OrderStatus;\nelseif choice = '3' then\n  \
      \  callframe Help;\nelse\n    message 'Invalid choice. Please try again.';\nendif;\n\
      ```\nThis code prompts for a menu choice and calls different frames based on the\
      \ user's input."
  - question: How to use the if statement to handle null values in Actian 4GL OpenROAD
      If Then Else Statement?
    answer: "Here's how to handle null values:\n```\nif customer.name is null then\n\
      \    display_name = 'Unknown Customer';\nelse\n    display_name = customer.name;\n\
      endif;\n```\nThis code checks if customer.name is null and assigns a default value\
      \ if it is."
  - question: Write code to implement a simple discount system using Actian 4GL OpenROAD
      If Then Else Statement.
    answer: "Here's a simple discount system implementation:\n```\nif total_purchase\
      \ > 1000 then\n    discount = 0.15;\nelseif total_purchase > 500 then\n    discount\
      \ = 0.10;\nelseif total_purchase > 100 then\n    discount = 0.05;\nelse\n    discount\
      \ = 0;\nendif;\nfinal_price = total_purchase * (1 - discount);\n```\nThis code\
      \ calculates a discount based on the total purchase amount."
  - question: How to use the if statement for data type checking in Actian 4GL OpenROAD
      If Then Else Statement?
    answer: "Here's how to check data types:\n```\nif typeof(input_value) = 'integer'\
      \ then\n    result = input_value * 2;\nelseif typeof(input_value) = 'float' then\n\
      \    result = input_value / 2;\nelse\n    message 'Invalid data type';\nendif;\n\
      ```\nThis code performs different operations based on the data type of input_value."
  - question: Write code to implement a simple login system using Actian 4GL OpenROAD
      If Then Else Statement.
    answer: "Here's a simple login system implementation:\n```\nusername = prompt 'Enter\
      \ username';\npassword = prompt 'Enter password';\nif username = 'admin' and password\
      \ = 'secret' then\n    message 'Login successful';\n    callframe AdminPanel;\n\
      else\n    message 'Invalid username or password';\n    callframe LoginRetry;\n\
      endif;\n```\nThis code checks the entered username and password and acts accordingly."
  - question: How to use the if statement for string comparison in Actian 4GL OpenROAD
      If Then Else Statement?
    answer: "Here's how to compare strings:\n```\nif lowercase(user_input) = 'yes' then\n\
      \    proceed = TRUE;\nelseif lowercase(user_input) = 'no' then\n    proceed =\
      \ FALSE;\nelse\n    message 'Invalid input. Please enter Yes or No.';\nendif;\n\
      ```\nThis code compares the lowercase version of user_input to 'yes' and 'no'."
  - question: Write code to implement a simple calculator using Actian 4GL OpenROAD
      If Then Else Statement.
    answer: "Here's a simple calculator implementation:\n```\nnum1 = prompt 'Enter first\
      \ number';\nnum2 = prompt 'Enter second number';\noperation = prompt 'Enter operation\
      \ (+, -, *, /)';\nif operation = '+' then\n    result = num1 + num2;\nelseif operation\
      \ = '-' then\n    result = num1 - num2;\nelseif operation = '*' then\n    result\
      \ = num1 * num2;\nelseif operation = '/' then\n    if num2 != 0 then\n       \
      \ result = num1 / num2;\n    else\n        message 'Error: Division by zero';\n\
      \    endif;\nelse\n    message 'Invalid operation';\nendif;\nif result is not\
      \ null then\n    message 'Result: ' || char(result);\nendif;\n```\nThis code performs\
      \ basic arithmetic operations based on user input."
  - question: How to use the if statement for date comparison in Actian 4GL OpenROAD
      If Then Else Statement?
    answer: "Here's how to compare dates:\n```\nif current_date > due_date then\n  \
      \  message 'Payment overdue';\nelseif current_date = due_date then\n    message\
      \ 'Payment due today';\nelse\n    days_left = due_date - current_date;\n    message\
      \ 'Payment due in ' || char(days_left) || ' days';\nendif;\n```\nThis code compares\
      \ the current date with a due date and provides appropriate messages."
  - question: Write code to implement a simple password strength checker using Actian
      4GL OpenROAD If Then Else Statement.
    answer: "Here's a simple password strength checker:\n```\npassword = prompt 'Enter\
      \ a password';\nif length(password) < 8 then\n    strength = 'Weak';\nelseif length(password)\
      \ >= 8 and length(password) < 12 then\n    strength = 'Medium';\nelseif length(password)\
      \ >= 12 then\n    strength = 'Strong';\nendif;\nmessage 'Password strength: '\
      \ || strength;\n```\nThis code categorizes password strength based on its length."
  - question: How to use the if statement for file handling in Actian 4GL OpenROAD If
      Then Else Statement?
    answer: "Here's how to handle file operations:\n```\nfilename = prompt 'Enter filename';\n\
      if file_exists(filename) then\n    if file_writable(filename) then\n        message\
      \ 'File can be modified';\n    else\n        message 'File is read-only';\n  \
      \  endif;\nelse\n    message 'File does not exist';\nendif;\n```\nThis code checks\
      \ if a file exists and if it's writable."
  - question: Write code to implement a simple age category classifier using Actian
      4GL OpenROAD If Then Else Statement.
    answer: "Here's a simple age category classifier:\n```\nage = prompt 'Enter age';\n\
      if age < 0 then\n    category = 'Invalid age';\nelseif age < 13 then\n    category\
      \ = 'Child';\nelseif age < 20 then\n    category = 'Teenager';\nelseif age < 65\
      \ then\n    category = 'Adult';\nelse\n    category = 'Senior';\nendif;\nmessage\
      \ 'Age category: ' || category;\n```\nThis code classifies a person into age categories\
      \ based on their age."
