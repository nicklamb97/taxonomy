version: 2
task_description: Write Actian 4GL OpenROAD If-Then-Else Statements
created_by: willsjames
seed_examples:
  - question: How do you write a basic if-then-endif statement in Actian 4GL OpenROAD
      If-Then-Else Statement?
    answer: "Write a basic if-then-endif statement like this:\n```\nif empnum = 0 then\n\
      \    employee_number = 'None';\nendif;\n```"
  - question: How can you use the if-then-else-endif structure in Actian 4GL OpenROAD
      If-Then-Else Statement?
    answer: "Use the if-then-else-endif structure like this:\n```\nif empnum = 0 then\n\
      \    enum = prompt 'Please enter employee number';\nelse\n    process_employee(empnum);\n\
      endif;\n```"
  - question: How would you implement an if-then-elseif-then-else-endif structure in
      Actian 4GL OpenROAD If-Then-Else Statement?
    answer: "Implement an if-then-elseif-then-else-endif structure like this:\n```\n\
      if status = 'n' then\n    callframe new;\nelseif status = 'c' then\n    callframe\
      \ completed;\nelse\n    callframe inprogress;\nendif;\n```"
  - question: How can you nest multiple if statements in Actian 4GL OpenROAD If-Then-Else
      Statement?
    answer: "Nest multiple if statements like this:\n```\nif status = 'n' then\n   \
      \ if empnum = 0 then\n        employee_number = 'None';\n    else\n        callframe\
      \ NewEmp;\n    endif;\nendif;\n```"
  - question: How would you use a complex condition in an Actian 4GL OpenROAD If-Then-Else
      Statement?
    answer: "Use a complex condition in an if statement like this:\n```\nif (age >=\
      \ 18 and status = 'active') or (age >= 65 and status = 'retired') then\n    eligible\
      \ = TRUE;\nelse\n    eligible = FALSE;\nendif;\n```"
