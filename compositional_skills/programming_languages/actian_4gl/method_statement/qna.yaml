version: 2
task_description: Write Actian 4GL OpenROAD Method Statement
created_by: willsjames
seed_examples:
  - question: How do you define a method with parameters in Actian 4GL OpenROAD Method
      Statement?
    answer: "Define a method with parameters like this:\n            method addtax(tax\
      \ = float) =\nbegin\nCurObject.amount = CurObject.amount + CurObject.amount *\
      \ tax;\nend;"
  - question: How can you create a method without parameters in Actian 4GL OpenROAD
      Method Statement?
    answer: "Create a method without parameters like this:\n            method printmsg\
      \ =\nbegin\nCurMethod.Trace(text = CurObject.FirstName);\nend;"
  - question: How would you define a method with multiple parameters in Actian 4GL OpenROAD
      Method Statement?
    answer: "Define a method with multiple parameters like this:\n            method\
      \ calculateTotal(price = float, quantity = integer) =\nbegin\nCurObject.total\
      \ = price * quantity;\nend;"
  - question: How can you use a declare block in an Actian 4GL OpenROAD Method Statement?
    answer: "Use a declare block in a method like this:\n            method processData(input\
      \ = varchar) =\ndeclare\ntemp = integer;\nbegin\ntemp = length(input);\nCurObject.result\
      \ = temp * 2;\nend;"
  - question: How would you return a value from an Actian 4GL OpenROAD Method Statement?
    answer: "Return a value from a method like this:\n            method calculateArea(width\
      \ = float, height = float) = float;\nbegin\nreturn width * height;\nend;"
