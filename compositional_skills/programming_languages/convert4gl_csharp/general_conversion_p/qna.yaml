version: 2
task_description: Learn about converting Actian 4GL SELECT statements for OpenROAD to C#.
created_by: nicklamb97
seed_examples:
  - question: "How would you convert the following 4GL Select statement into C#? Respond only in code:
    SELECT product_id, product_name, price FROM products WHERE category = 'Electronics';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT product_id, product_name, price FROM products WHERE category = @category",
              connection))
          {
              command.Parameters.AddWithValue("@category", "Electronics");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int productId = (int)reader["product_id"];
                      string productName = reader["product_name"].ToString();
                      decimal price = (decimal)reader["price"];
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT customer_id, customer_name FROM customers WHERE city = 'New York';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT customer_id, customer_name FROM customers WHERE city = @city",
              connection))
          {
              command.Parameters.AddWithValue("@city", "New York");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int customerId = (int)reader["customer_id"];
                      string customerName = reader["customer_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT order_id, order_date FROM orders WHERE total_amount > 1000;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT order_id, order_date FROM orders WHERE total_amount > @amount",
              connection))
          {
              command.Parameters.AddWithValue("@amount", 1000);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int orderId = (int)reader["order_id"];
                      DateTime orderDate = (DateTime)reader["order_date"];
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT employee_id, first_name, last_name FROM employees WHERE hire_date > '2022-01-01';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT employee_id, first_name, last_name FROM employees WHERE hire_date > @hireDate",
              connection))
          {
              command.Parameters.AddWithValue("@hireDate", new DateTime(2022, 1, 1));
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int employeeId = (int)reader["employee_id"];
                      string firstName = reader["first_name"].ToString();
                      string lastName = reader["last_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT product_id, product_name FROM products WHERE stock_quantity < 10;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT product_id, product_name FROM products WHERE stock_quantity < @quantity",
              connection))
          {
              command.Parameters.AddWithValue("@quantity", 10);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int productId = (int)reader["product_id"];
                      string productName = reader["product_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT customer_id, customer_name FROM customers WHERE email LIKE '%@gmail.com';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT customer_id, customer_name FROM customers WHERE email LIKE @emailPattern",
              connection))
          {
              command.Parameters.AddWithValue("@emailPattern", "%@gmail.com");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int customerId = (int)reader["customer_id"];
                      string customerName = reader["customer_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT order_id, customer_id FROM orders WHERE order_date BETWEEN '2023-01-01' AND '2023-12-31';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT order_id, customer_id FROM orders WHERE order_date BETWEEN @startDate AND @endDate",
              connection))
          {
              command.Parameters.AddWithValue("@startDate", new DateTime(2023, 1, 1));
              command.Parameters.AddWithValue("@endDate", new DateTime(2023, 12, 31));
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int orderId = (int)reader["order_id"];
                      int customerId = (int)reader["customer_id"];
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT product_id, product_name FROM products WHERE category IN ('Books', 'Electronics');"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT product_id, product_name FROM products WHERE category IN (@category1, @category2)",
              connection))
          {
              command.Parameters.AddWithValue("@category1", "Books");
              command.Parameters.AddWithValue("@category2", "Electronics");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int productId = (int)reader["product_id"];
                      string productName = reader["product_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT employee_id, first_name, last_name FROM employees WHERE department_id IS NULL;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT employee_id, first_name, last_name FROM employees WHERE department_id IS NULL",
              connection))
          {
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int employeeId = (int)reader["employee_id"];
                      string firstName = reader["first_name"].ToString();
                      string lastName = reader["last_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT customer_id, customer_name FROM customers WHERE credit_limit > 5000 ORDER BY customer_name;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT customer_id, customer_name FROM customers WHERE credit_limit > @limit ORDER BY customer_name",
              connection))
          {
              command.Parameters.AddWithValue("@limit", 5000);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int customerId = (int)reader["customer_id"];
                      string customerName = reader["customer_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT product_id, product_name, price FROM products WHERE price BETWEEN 10 AND 50;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT product_id, product_name, price FROM products WHERE price BETWEEN @minPrice AND @maxPrice",
              connection))
          {
              command.Parameters.AddWithValue("@minPrice", 10);
              command.Parameters.AddWithValue("@maxPrice", 50);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int productId = (int)reader["product_id"];
                      string productName = reader["product_name"].ToString();
                      decimal price = (decimal)reader["price"];
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT order_id, customer_id, order_date FROM orders WHERE EXTRACT(YEAR FROM order_date) = 2023;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT order_id, customer_id, order_date FROM orders WHERE YEAR(order_date) = @year",
              connection))
          {
              command.Parameters.AddWithValue("@year", 2023);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int orderId = (int)reader["order_id"];
                      int customerId = (int)reader["customer_id"];
                      DateTime orderDate = (DateTime)reader["order_date"];
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT employee_id, first_name, last_name FROM employees WHERE salary > (SELECT AVG(salary) FROM employees);"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT employee_id, first_name, last_name FROM employees WHERE salary > (SELECT AVG(salary) FROM employees)",
              connection))
          {
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int employeeId = (int)reader["employee_id"];
                      string firstName = reader["first_name"].ToString();
                      string lastName = reader["last_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT customer_id, customer_name FROM customers WHERE customer_id NOT IN (SELECT customer_id FROM orders);"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT customer_id, customer_name FROM customers WHERE customer_id NOT IN (SELECT customer_id FROM orders)",
              connection))
          {
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int customerId = (int)reader["customer_id"];
                      string customerName = reader["customer_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT product_id, product_name FROM products WHERE EXISTS (SELECT 1 FROM order_items WHERE order_items.product_id = products.product_id);"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT product_id, product_name FROM products WHERE EXISTS (SELECT 1 FROM order_items WHERE order_items.product_id = products.product_id)",
              connection))
          {
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int productId = (int)reader["product_id"];
                      string productName = reader["product_name"].ToString();
                      // Process data
                  }
              }
          }
      }
