version: 2
task_description: Learn about converting Actian 4GL SELECT statements for OpenROAD to C#.
created_by: nicklamb97
seed_examples:
  - question: "How would you convert the following 4GL Select statement into C#? Respond only in code:
    SELECT product_id, product_name, price FROM products WHERE category = 'Electronics';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT product_id, product_name, price FROM products WHERE category = @category",
              connection))
          {
              command.Parameters.AddWithValue("@category", "Electronics");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int productId = (int)reader["product_id"];
                      string productName = reader["product_name"].ToString();
                      decimal price = (decimal)reader["price"];
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT customer_id, customer_name FROM customers WHERE city = 'New York';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT customer_id, customer_name FROM customers WHERE city = @city",
              connection))
          {
              command.Parameters.AddWithValue("@city", "New York");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int customerId = (int)reader["customer_id"];
                      string customerName = reader["customer_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT order_id, order_date FROM orders WHERE total_amount > 1000;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT order_id, order_date FROM orders WHERE total_amount > @amount",
              connection))
          {
              command.Parameters.AddWithValue("@amount", 1000);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int orderId = (int)reader["order_id"];
                      DateTime orderDate = (DateTime)reader["order_date"];
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT employee_id, first_name, last_name FROM employees WHERE department = 'Sales';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT employee_id, first_name, last_name FROM employees WHERE department = @dept",
              connection))
          {
              command.Parameters.AddWithValue("@dept", "Sales");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int employeeId = (int)reader["employee_id"];
                      string firstName = reader["first_name"].ToString();
                      string lastName = reader["last_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT book_id, title, author FROM books WHERE genre = 'Science Fiction';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT book_id, title, author FROM books WHERE genre = @genre",
              connection))
          {
              command.Parameters.AddWithValue("@genre", "Science Fiction");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int bookId = (int)reader["book_id"];
                      string title = reader["title"].ToString();
                      string author = reader["author"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT student_id, student_name FROM students WHERE grade > 90;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT student_id, student_name FROM students WHERE grade > @grade",
              connection))
          {
              command.Parameters.AddWithValue("@grade", 90);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int studentId = (int)reader["student_id"];
                      string studentName = reader["student_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT project_id, project_name FROM projects WHERE start_date > '2023-01-01';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT project_id, project_name FROM projects WHERE start_date > @startDate",
              connection))
          {
              command.Parameters.AddWithValue("@startDate", new DateTime(2023, 1, 1));
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int projectId = (int)reader["project_id"];
                      string projectName = reader["project_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT car_id, make, model FROM cars WHERE year >= 2020;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT car_id, make, model FROM cars WHERE year >= @year",
              connection))
          {
              command.Parameters.AddWithValue("@year", 2020);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int carId = (int)reader["car_id"];
                      string make = reader["make"].ToString();
                      string model = reader["model"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT supplier_id, supplier_name FROM suppliers WHERE country = 'Germany';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT supplier_id, supplier_name FROM suppliers WHERE country = @country",
              connection))
          {
              command.Parameters.AddWithValue("@country", "Germany");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int supplierId = (int)reader["supplier_id"];
                      string supplierName = reader["supplier_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT movie_id, title FROM movies WHERE release_year = 2022;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT movie_id, title FROM movies WHERE release_year = @year",
              connection))
          {
              command.Parameters.AddWithValue("@year", 2022);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int movieId = (int)reader["movie_id"];
                      string title = reader["title"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT event_id, event_name FROM events WHERE date_time < '2023-12-31 23:59:59';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT event_id, event_name FROM events WHERE date_time < @dateTime",
              connection))
          {
              command.Parameters.AddWithValue("@dateTime", new DateTime(2023, 12, 31, 23, 59, 59));
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int eventId = (int)reader["event_id"];
                      string eventName = reader["event_name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT product_id, name FROM products WHERE stock_quantity < 10;"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT product_id, name FROM products WHERE stock_quantity < @quantity",
              connection))
          {
              command.Parameters.AddWithValue("@quantity", 10);
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int productId = (int)reader["product_id"];
                      string name = reader["name"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT user_id, username FROM users WHERE last_login > '2023-06-01';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT user_id, username FROM users WHERE last_login > @lastLogin",
              connection))
          {
              command.Parameters.AddWithValue("@lastLogin", new DateTime(2023, 6, 1));
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int userId = (int)reader["user_id"];
                      string username = reader["username"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT task_id, description FROM tasks WHERE status = 'Completed';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT task_id, description FROM tasks WHERE status = @status",
              connection))
          {
              command.Parameters.AddWithValue("@status", "Completed");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int taskId = (int)reader["task_id"];
                      string description = reader["description"].ToString();
                      // Process data
                  }
              }
          }
      }

  - question: "How would you convert this 4GL Select statement into C#? Respond only in code:
    SELECT article_id, title FROM articles WHERE category = 'Technology';"
    answer: |
      using (SqlConnection connection = new SqlConnection(connectionString))
      {
          connection.Open();
          using (SqlCommand command = new SqlCommand(
              "SELECT article_id, title FROM articles WHERE category = @category",
              connection))
          {
              command.Parameters.AddWithValue("@category", "Technology");
              using (SqlDataReader reader = command.ExecuteReader())
              {
                  while (reader.Read())
                  {
                      int articleId = (int)reader["article_id"];
                      string title = reader["title"].ToString();
                      // Process data
                  }
              }
          }
      }
